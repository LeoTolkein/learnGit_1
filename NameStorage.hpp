// the demonstration of git_2
#include <string>

class PersonName {
private:
  std::string Name;

public:
  PersonName (const string &NameOfPerson);
  void setName (const string &NameOfPerson);
  virtual ~PersonName ();
};
